# App UX Round 2 (Calendar) Implementation Plan

> Execute this plan with the agentic-workers `executing-plans` skill (subagent-driven TDD, review checkpoints per task).

- 날짜: 2026-06-16
- 소스 오브 트루스: `여행관리앱_설계서.md` §4.2/§5.1/§8/§10.2, `CLAUDE.md` §5/§6/§7, `.claude/rules/web-stack.md`, `.claude/rules/security-privacy.md`, `.claude/rules/ux-and-accessibility.md`.
- 스펙: `docs/superpowers/specs/2026-06-16-ux-overhaul-design.md` (R2 캘린더 섹션).
- 조사(verbatim): `docs/superpowers/plans/.research-r2cal/01-core.md`, `02-reminders.md`, `03-perms-conflict.md`, `04-link-views.md`.

## Goal

R2 캘린더를 "실앱 수준"으로 올린다: 반복 일정 3-범위 편집/삭제(이 일정만/이후/전체), 안 울리는 리마인더를 **인앱 다가오는 일정 피드**로, 상대 PERSONAL 일정 읽기 전용 + 권한거부와 버전충돌 구분 + 충돌 시 입력보존·메모 머지, **요일(타임라인) 뷰**(가능하면 주 뷰), 월 셀 제목 칩, 시간 검증(end<start·자정 넘김·종일 다일), 캘린더↔장소 연결 UI + 빈 아젠다 CTA, 로딩 스켈레톤·연결됨/미연결 구분, 트랙 범례 색+이름칩, EventSheet reduce-motion + 스와이프 다운 닫기.

> 스펙(`2026-06-16-ux-overhaul-design.md:48`)의 R2 항목 중 **"타임존(여행 현지시각)"은 의식적으로 R2 범위 밖**으로 둔다(R4 캘린더 후속 태스크 소유) — 본 라운드는 고정 표시 tz(Asia/Seoul) 위에 일-버킷 일치를 먼저 확보. 근거·이관 범위는 하단 「정직성(검증 한계)」 참조.

> R1.5에서 이미 출하됨(재구현 금지): 일정 삭제 인라인 확인 + `되돌리기` Undo 토스트 + `useRestoreEvent` (`CalendarPage.tsx:104-116`, `EventSheet.tsx` `confirmingDelete`).

## Architecture

- **순수 함수 우선(테스트로 못박음)**: 반복 범위 연산(`rrule.ts`의 `buildRule`/`parseRule`/`expandEvents` + 신규 `weekMatrix`/`minuteOfDay`/`buildUpcomingFeed`)은 `src/lib/calendar/`에 두고 `src/__tests__/`에서 단위 커버. UI는 그 출력을 렌더.
- **낙관적 락 유지**: 모든 쓰기는 `versionedUpdate`/`softDelete`(`version` 조건부, 0행=충돌). 3-범위 멀티라이트는 **순서 + 부분실패 메시지**로 처리(트랜잭션 RPC 없음 — 조사 01 §6). 다중 라이트는 EXDATE 먼저(되돌리기 가능), 그다음 override INSERT.
- **RLS = 진짜 방어선**: 상대 PERSONAL 편집은 `events_update USING (visibility='SHARED' OR owner_id=auth.uid())`로 DB가 0행 처리 → UI에서 `canEdit`로 사전 차단(조사 03 §4) + override INSERT는 `owner_id=myId`(insert USING). events 테이블/컬럼은 0001에 이미 존재 → **RLS/스키마 변경 없음**(아래 Task에서 마이그레이션 불필요 명시). EXDATE override 가시성은 기존 `events_select`(couple_id + deleted_at IS NULL)로 충분 — override는 평범한 비반복 행.
- **Realtime 무료**: `useEvents`가 이미 `events:${coupleId}` 구독 + `['events',coupleId]` 무효화 → 피드/캘린더 라이브 갱신(신규 채널 금지, 조사 02 §3).
- **a11y 보존**: 색+심볼+라벨 이중화(●▲■ / 함께·나·상대), `role="dialog"`/focus-trap/Esc, `aria-live`, `prefers-reduced-motion`, 다크모드 토큰.

## Tech Stack

React 18 + Vite + TS(strict) / Supabase(Postgres+RLS+Realtime) / TanStack Query / vitest / Playwright. 명령: typecheck=`npm run typecheck`, 단일 테스트=`npm run test -- src/__tests__/<file>`, build=`npm run build`, e2e=`npm run e2e`, 마이그레이션=`npx supabase db push`.

---

## Phase R2.1: 순수 함수 토대 (반복 범위·시간검증·주/일 뷰 헬퍼·피드)

### Task 1: 시간 검증 + 자정 넘김/종일 다일 순수 헬퍼 (`buildEventTimes`)

**Files:**
- `src/lib/calendar/eventTimes.ts` (신규)
- `src/__tests__/eventTimes.test.ts` (신규)

EventSheet의 `toIso`(`EventSheet.tsx:9-11`)는 start/end에 **같은 `date`**를 써 `end<start`(23:00→01:00)를 만들고 DB `CHECK ("end" >= start)`(0001:207)에 막혀 raw Postgres 에러. 순수 헬퍼로 자정 넘김 자동 다음날 롤 + 검증 분리.

- [ ] 실패 테스트 작성 `src/__tests__/eventTimes.test.ts`: import `{ buildEventTimes }` from `@/lib/calendar/eventTimes`. 케이스:
  - 종일: `buildEventTimes({date:'2026-06-16', allDay:true})` → `{ start:'2026-06-15T15:00:00.000Z', end:'2026-06-16T14:59:00.000Z', ok:true }` (KST 00:00→23:59).
  - 정상 시간: `buildEventTimes({date:'2026-06-16', allDay:false, startTime:'10:00', endTime:'12:00'})` → `ok:true`, `new Date(end) > new Date(start)`.
  - 자정 넘김(roll): `buildEventTimes({date:'2026-06-16', allDay:false, startTime:'23:00', endTime:'01:00'})` → `ok:true` 이고 `end`가 start+2h(다음날 01:00 KST = `2026-06-16T16:00:00.000Z`).
  - 같은 시각: `startTime==='10:00' && endTime==='10:00'` → `ok:false, reason:'same'`(0길이 거부; DB는 `>=` 허용이지만 0길이 일정은 사용자 실수로 차단).
  - 종일 다일: `buildEventTimes({date:'2026-06-16', allDay:true, endDate:'2026-06-18'})` → `ok:true`, start=06-16 00:00 KST, end=06-18 23:59 KST.
  - 종일 endDate<date → `ok:false, reason:'range'`.
- [ ] 실행: `npm run test -- src/__tests__/eventTimes.test.ts` → FAIL (모듈 없음).
- [ ] 최소 구현 `src/lib/calendar/eventTimes.ts` (완전 코드):
```ts
// 일정 시각 빌드 + 검증 — 순수 함수(테스트로 못박음).
// Asia/Seoul 고정 오프셋(+09:00, DST 없음)으로 벽시계→ISO. end<=start면 자정 넘김으로 보고
// end를 다음날로 롤(종일 아님). 0길이/역전 범위는 거부(ok:false). DB CHECK("end">=start)는 백스톱.
export type BuildEventTimesInput = {
  date: string // 'YYYY-MM-DD' 시작일(또는 종일 시작)
  allDay: boolean
  startTime?: string // 'HH:mm' (allDay=false)
  endTime?: string // 'HH:mm' (allDay=false)
  endDate?: string // 'YYYY-MM-DD' 종일 다일 종료일(없으면 date와 동일)
}
export type BuildEventTimesResult =
  | { ok: true; start: string; end: string }
  | { ok: false; reason: 'same' | 'range' | 'missing' }

function iso(date: string, time: string): string {
  return new Date(`${date}T${time}:00+09:00`).toISOString()
}
function addDayKey(key: string): string {
  const [y, m, d] = key.split('-').map(Number)
  const t = new Date(Date.UTC(y!, (m ?? 1) - 1, d! + 1))
  const p = (n: number) => (n < 10 ? `0${n}` : `${n}`)
  return `${t.getUTCFullYear()}-${p(t.getUTCMonth() + 1)}-${p(t.getUTCDate())}`
}

export function buildEventTimes(input: BuildEventTimesInput): BuildEventTimesResult {
  if (input.allDay) {
    const endKey = input.endDate && input.endDate.trim() ? input.endDate : input.date
    if (endKey < input.date) return { ok: false, reason: 'range' }
    return { ok: true, start: iso(input.date, '00:00'), end: iso(endKey, '23:59') }
  }
  const st = input.startTime
  const et = input.endTime
  if (!st || !et) return { ok: false, reason: 'missing' }
  if (st === et) return { ok: false, reason: 'same' }
  const start = iso(input.date, st)
  // end<=start면 자정 넘김 → 종료를 다음날 같은 시각으로
  const endSameDay = iso(input.date, et)
  const end = endSameDay <= start ? iso(addDayKey(input.date), et) : endSameDay
  return { ok: true, start, end }
}
```
- [ ] 실행: `npm run test -- src/__tests__/eventTimes.test.ts` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 일정 시각 빌드·검증 순수헬퍼(자정 넘김·종일 다일)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 2: `buildRule`에 UNTIL 방출 + 반복 범위 연산 (`recurrenceScope.ts`)

**Files:**
- `src/lib/calendar/rrule.ts:53-58` (buildRule 시그니처 확장)
- `src/lib/calendar/recurrenceScope.ts` (신규)
- `src/__tests__/rrule.test.ts` (UNTIL 라운드트립 추가)
- `src/__tests__/recurrenceScope.test.ts` (신규)

조사 01 §1: `buildRule`은 UNTIL을 방출하지 않아 "이후" 분할에서 손실. `parseRule`은 이미 UNTIL을 읽음. 3-범위 연산을 한 곳에 모은다(`exdateOccurrence`/`splitFollowing`).

- [ ] 실패 테스트 추가 `src/__tests__/rrule.test.ts`: `buildRule('WEEKLY', 1, undefined, undefined, '2026-08-01T00:00:00Z')` 결과를 `parseRule`로 라운드트립 → `{ freq:'WEEKLY', interval:1, until:'2026-08-01T00:00:00Z' }`. (기존 EXDATE/COUNT 케이스는 유지.)
- [ ] 실패 테스트 작성 `src/__tests__/recurrenceScope.test.ts`: import `{ exdateOccurrence, splitFollowing }` from `@/lib/calendar/recurrenceScope`. 케이스:
  - `exdateOccurrence('FREQ=DAILY;INTERVAL=1;COUNT=5', '2026-06-17')` → `'FREQ=DAILY;INTERVAL=1;COUNT=5;EXDATE=2026-06-17'` (dayKey append, 중복 무시).
  - 기존 EXDATE 보존: `exdateOccurrence('FREQ=DAILY;INTERVAL=1;EXDATE=2026-06-16', '2026-06-17')` → EXDATE에 두 키 모두.
  - `splitFollowing` — 시리즈 start `seriesStart='2026-06-16T01:00:00Z'`(KST 10:00) DAILY, 분할 occurrence 시작 `occStartIso='2026-06-18T01:00:00Z'`(dayKey `2026-06-18`) → `{ truncatedRule, newSeriesStartKey:'2026-06-18' }`. 그다음 절단 시리즈를 펼쳐 06-16, 06-17만 남는지(분할일 이전까지) 검증. ⚠️ `expandOccurrences`의 실제 시그니처는 `(startIso, rule, winStartIso, winEndIso)` 4-인자(`rrule.ts:47`)이고 `parseRule`은 `ParsedRule | null`을 반환하므로 **윈도우 양끝 + 비-null 단언**을 함께 쓴다: `const win = { start: '2026-06-16T00:00:00Z', end: '2026-06-25T00:00:00Z' }; const rule = parseRule(truncatedRule); expect(rule).not.toBeNull(); expect(expandOccurrences(seriesStart, rule!, win.start, win.end).map((s) => dayKey(s))).toEqual(['2026-06-16', '2026-06-17'])` (분할 occurrence가 들어간 06-18 이후는 절단으로 제외). UNTIL = 분할 occurrence 시작 직전(1ms 전 ISO)으로 절단 — 구현은 UNTIL 사용(아래). 테스트는 `import { dayKey } from '@/lib/calendar/eventDays'` 및 `import { parseRule, expandOccurrences } from '@/lib/calendar/rrule'` 추가.
- [ ] 실행: `npm run test -- src/__tests__/recurrenceScope.test.ts` → FAIL.
- [ ] `rrule.ts` `buildRule` 확장(verbatim 기반):
```ts
export function buildRule(freq: Freq, interval: number, count?: number, exdates?: string[], until?: string): string {
  let s = `FREQ=${freq};INTERVAL=${Math.max(1, interval)}`
  if (count && count > 0) s += `;COUNT=${count}`
  if (until) s += `;UNTIL=${until}`
  if (exdates && exdates.length > 0) s += `;EXDATE=${exdates.join(',')}`
  return s
}
```
- [ ] 최소 구현 `src/lib/calendar/recurrenceScope.ts`:
```ts
// 반복 일정 3-범위(이 일정만/이후/전체) 연산 — 순수 함수. 'all'은 시리즈 plain update(여기 없음).
import { parseRule, buildRule } from './rrule'
import { dayKey } from './eventDays'

/** '이 일정만' 삭제/오버라이드: 해당 occurrence dayKey를 시리즈 EXDATE에 추가한 RRULE 반환. */
export function exdateOccurrence(rule: string, occDayKey: string): string {
  const p = parseRule(rule)
  if (!p) return rule
  const ex = new Set(p.exdates)
  ex.add(occDayKey)
  return buildRule(p.freq, p.interval, p.count, Array.from(ex).sort(), p.until)
}

export type SplitResult = { truncatedRule: string; newSeriesStartKey: string }

/** '이후': 분할 occurrence(occStartIso) 직전까지로 시리즈를 절단(UNTIL)하고, 분할일을 새 시리즈 시작키로. */
export function splitFollowing(rule: string, occStartIso: string): SplitResult {
  const p = parseRule(rule)
  if (!p) return { truncatedRule: rule, newSeriesStartKey: dayKey(occStartIso) }
  // 분할 occurrence 시작 1ms 전을 UNTIL로(그 회차는 새 시리즈가 가져감). COUNT는 제거(UNTIL이 절단).
  const until = new Date(new Date(occStartIso).getTime() - 1).toISOString()
  const truncatedRule = buildRule(p.freq, p.interval, undefined, p.exdates, until)
  return { truncatedRule, newSeriesStartKey: dayKey(occStartIso) }
}
```
- [ ] 실행: `npm run test -- src/__tests__/recurrenceScope.test.ts` 및 `npm run test -- src/__tests__/rrule.test.ts` → PASS.
- [ ] `npm run typecheck`. (⚠️ combine-to-typecheck: `buildRule` 시그니처 변경은 `EventSheet.tsx:282` 호출부에 `until` 인자 추가가 필요 — Task 5에서 호출. 이 Task에서는 새 5번째 인자가 optional이라 기존 호출 타입 안전 유지됨을 typecheck로 확인.)
- [ ] 커밋: `feat(calendar): buildRule UNTIL 방출 + 반복 3-범위 연산(EXDATE·이후 분할)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 3: 주/일 뷰 순수 헬퍼 (`weekMatrix` · `minuteOfDay` · 시간 슬롯)

**Files:**
- `src/lib/calendar/eventDays.ts:181-211` (헬퍼 추가)
- `src/__tests__/calendar.test.ts` (주/분 헬퍼 케이스 추가)

조사 01 §2/04 §4: 주/일 뷰용 헬퍼 부재. `weekMatrix`(주 7일 dayKey)와 `minuteOfDay`(타임라인 위치)를 추가.

- [ ] 실패 테스트 추가 `src/__tests__/calendar.test.ts`: import `{ weekMatrix, minuteOfDay }`. 케이스:
  - `weekMatrix('2026-06-16')`(화요일) → 길이 7, `[0].key==='2026-06-14'`(일요일 시작), `[6].key==='2026-06-20'`, 각 `{key, day, inMonth:true}` 형태 재사용(`DayCell`은 inMonth 무의미 → true).
  - `minuteOfDay('2026-06-16T01:30:00Z')` → KST 10:30 = `630`.
  - `minuteOfDay('2026-06-15T15:00:00Z')` → KST 00:00 = `0`.
- [ ] 실행: `npm run test -- src/__tests__/calendar.test.ts` → FAIL.
- [ ] 최소 구현 `src/lib/calendar/eventDays.ts` 추가:
```ts
/** anchorKey('YYYY-MM-DD')가 속한 주(일요일 시작) 7칸. UTC 산술. */
export function weekMatrix(anchorKey: string): DayCell[] {
  const [y, m, d] = anchorKey.split('-').map(Number)
  const base = new Date(Date.UTC(y!, (m ?? 1) - 1, d!))
  const dow = base.getUTCDay() // 0=일
  const cells: DayCell[] = []
  for (let i = 0; i < 7; i++) {
    const cur = new Date(Date.UTC(y!, (m ?? 1) - 1, d! - dow + i))
    cells.push({ key: ymdKey(cur.getUTCFullYear(), cur.getUTCMonth(), cur.getUTCDate()), day: cur.getUTCDate(), inMonth: true })
  }
  return cells
}

/** ISO → 표시 tz 기준 자정으로부터의 분(타임라인 세로 위치용). 0..1439. */
export function minuteOfDay(iso: string, tz: string = DISPLAY_TZ): number {
  const hm = new Intl.DateTimeFormat('en-GB', { timeZone: tz, hour: '2-digit', minute: '2-digit', hour12: false }).format(new Date(iso))
  const [h, mi] = hm.split(':').map(Number)
  return (h ?? 0) * 60 + (mi ?? 0)
}
```
- [ ] 실행: `npm run test -- src/__tests__/calendar.test.ts` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 주 행렬·분-of-day 순수헬퍼(주/일 뷰 토대)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 4: 다가오는 일정/활동 피드 계산 (`buildUpcomingFeed`)

**Files:**
- `src/lib/calendar/upcomingFeed.ts` (신규)
- `src/__tests__/upcomingFeed.test.ts` (신규)

조사 02 §5: 안 울리는 리마인더를 인앱 피드로. `events`+`reminders`로 `D-N`/`곧 시작` 계산. 반복은 `expandEvents` 먼저.

- [ ] 실패 테스트 작성 `src/__tests__/upcomingFeed.test.ts`: import `{ buildUpcomingFeed }` from `@/lib/calendar/upcomingFeed`. 입력 `events: EventRow[]`, `nowIso`, `myId`. 케이스:
  - D-day: 3일 뒤 시작하는 이벤트 → `feed.find(i => i.kind==='dday')` 의 `label==='D-3'`, `title` 일치.
  - 곧 시작(내 리마인더): start = now+10분, `reminders:[{userId:'me', offsetMinutes:10}]`, myId='me' → `kind:'imminent'`, `label` 에 `'10분 뒤'` 포함, `minutesUntil===10`.
  - 상대 리마인더는 나에게 imminent 아님: 같은 이벤트 `reminders:[{userId:'partner', offsetMinutes:10}]`, myId='me' → imminent 항목 없음(단 60분 이내면 soft imminent 가능 — 테스트는 start=now+90분으로 두어 imminent 0 보장).
  - 반복 전개: WEEKLY 시리즈(시리즈 start 과거), 다음 occurrence가 now+2일 → `dday` 항목 `D-2`(시리즈 start 아님). `expandEvents` 경유 검증.
  - 과거/지난 일정 제외, cap N(기본 5) 적용.
  - dedupe: 같은 이벤트가 imminent이자 today면 imminent만(중복 1회).
- [ ] 실행: `npm run test -- src/__tests__/upcomingFeed.test.ts` → FAIL.
- [ ] 최소 구현 `src/lib/calendar/upcomingFeed.ts`:
```ts
// 다가오는 일정/활동 피드 — 순수 함수(테스트로 못박음). 안 울리는 리마인더를 인앱 신호로.
// (웹푸시는 PWA 홈화면+iOS16.4 한정 → 후속. 인앱 피드가 1차 알림 수단 — ux-and-accessibility §6.)
import type { EventRow } from '@/hooks/useEvents'
import { expandEvents } from './rrule'
import { dayKey, dDayLabel, upcomingEvents } from './eventDays'

export type FeedItem = {
  id: string // `${eventId}:${startIso}`
  kind: 'imminent' | 'dday'
  title: string
  startIso: string
  label: string // 'D-3' | '오늘' | '내일' | '10분 뒤'
  minutesUntil?: number
}

const DAY_MS = 86400000

export function buildUpcomingFeed(
  events: EventRow[], nowIso: string, myId: string | null, max = 5,
): FeedItem[] {
  const nowMs = new Date(nowIso).getTime()
  const winEnd = new Date(nowMs + 30 * DAY_MS).toISOString()
  const occ = expandEvents(events, nowIso, winEnd)
  const todayKey = dayKey(nowIso)

  const imminent: FeedItem[] = []
  for (const e of occ) {
    const startMs = new Date(e.start).getTime()
    if (startMs < nowMs) continue
    const myReminders = (e.reminders ?? []).filter((r) => r.userId === myId)
    const offsets = myReminders.map((r) => r.offsetMinutes)
    // 내 리마인더 점화창 또는 60분 이내 soft
    const minUntil = Math.round((startMs - nowMs) / 60000)
    const fired = offsets.some((off) => nowMs >= startMs - off * 60000) || minUntil <= 60
    if (fired) {
      imminent.push({
        id: `${e.id}:${e.start}`, kind: 'imminent', title: e.title, startIso: e.start,
        label: `${minUntil}분 뒤`, minutesUntil: minUntil,
      })
    }
  }
  imminent.sort((a, b) => a.startIso.localeCompare(b.startIso))

  const imminentIds = new Set(imminent.map((i) => i.id))
  const dday: FeedItem[] = upcomingEvents(occ, nowIso)
    .map((e) => ({
      id: `${e.id}:${e.start}`, kind: 'dday' as const, title: e.title, startIso: e.start,
      label: dDayLabel(dayKey(e.start), todayKey),
    }))
    .filter((i) => !imminentIds.has(i.id))

  return [...imminent, ...dday].slice(0, max)
}
```
- [ ] 실행: `npm run test -- src/__tests__/upcomingFeed.test.ts` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 다가오는 일정 피드 계산(반복 전개·D-N·곧 시작)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## Phase R2.2: EventSheet — 시간검증·권한·충돌보존·장소연결

### Task 5: EventSheet 시간 검증 + 인라인 에러 + 입력 보존

**Files:**
- `src/components/calendar/EventSheet.tsx:9-11` (toIso 제거 → buildEventTimes), `:74-110` (onSubmit), 종일 endDate 입력 추가
- `src/__tests__/eventSheet.test.tsx` (신규)

조사 01 §3: onSubmit은 빈 제목만 가드. `buildEventTimes`(Task 1)로 검증 → 인라인 에러(`role="alert"`), 입력 보존(로컬 state 유지), DB 도달 차단.

- [ ] 실패 테스트 작성 `src/__tests__/eventSheet.test.tsx`(RTL): 시트 렌더 후 종일 해제, startTime `10:00`, endTime `10:00` 입력 → 저장 클릭 → `onCreate`/`onUpdate` **미호출**, `screen.getByRole('alert')` 에 `시작·종료 시간이 같아요` 류 메시지. 입력값(제목)이 그대로 남아있음(보존). 그다음 endTime `12:00`으로 고치고 저장 → `onCreate` 1회 호출, payload `start<end`.
- [ ] 실행: `npm run test -- src/__tests__/eventSheet.test.tsx` → FAIL.
- [ ] 구현 `EventSheet.tsx`:
  - `toIso` import 제거, `import { buildEventTimes } from '@/lib/calendar/eventTimes'`.
  - `const [timeError, setTimeError] = useState<string | null>(null)`.
  - 종일 다일용 `const [endDate, setEndDate] = useState(initial ? dayKey(initial.end) : defaultDate)` + 종일 체크 시 종료일 `<input type="date">` 노출.
  - onSubmit 교체(verbatim 기반):
```ts
  const onSubmit = (e: FormEvent) => {
    e.preventDefault()
    const t = title.trim()
    if (!t) return
    const times = buildEventTimes({ date, allDay, startTime, endTime, endDate })
    if (!times.ok) {
      setTimeError(
        times.reason === 'same' ? '시작·종료 시간이 같아요. 종료를 더 늦게 잡아주세요.'
          : times.reason === 'range' ? '종료일이 시작일보다 빨라요.'
          : '시작·종료 시간을 입력해주세요.',
      )
      return
    }
    setTimeError(null)
    const { start, end } = times
    const cleanMemo = memo.trim() || null
    const recurrenceRule =
      recurrence === 'none' ? null : buildRule(recurrence, 1, recurCount > 0 ? recurCount : undefined, initRule?.exdates)
    const others = (initial?.reminders ?? []).filter((r) => r.userId !== myId)
    const reminders = myReminder > 0 && myId ? [...others, { userId: myId, offsetMinutes: myReminder }] : others
    if (editing && initial) {
      onUpdate(initial.id, initial.version, {
        title: t, start, end, is_all_day: allDay, visibility, memo: cleanMemo,
        place_id: placeId, recurrence_rule: recurrenceRule, reminders,
      })
    } else {
      onCreate({
        title: t, start, end, isAllDay: allDay, timeZone: DISPLAY_TZ,
        visibility, memo: cleanMemo, placeId, recurrenceRule, reminders,
      })
    }
  }
```
  - 폼에 `{timeError && <p role="alert" className={styles.formError}>{timeError}</p>}` 추가(저장 버튼 위). (`placeId`는 Task 8에서 state 추가 — 이 Task에선 `place_id: initial?.place_id ?? null` 임시 상수로 두고 Task 8에서 state로 승격. ⚠️ combine-to-typecheck: placeId 미정의 회피 위해 이 Task에서 `const placeId = initial?.place_id ?? null` 선언.)
- [ ] 실행: `npm run test -- src/__tests__/eventSheet.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): EventSheet 시간 검증·인라인 에러·입력 보존(자정 넘김 헬퍼)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 6: 상대 PERSONAL 일정 읽기 전용 + canEdit 가드

**Files:**
- `src/components/calendar/EventSheet.tsx:13-25` (props/derive), 입력·버튼 가드
- `src/__tests__/eventSheetReadonly.test.tsx` (신규)

조사 03 §4: `canEdit = visibility==='SHARED' || owner_id===myId`(RLS USING 미러). 상대 PERSONAL이면 저장/삭제 숨김 + `상대 일정` 라벨. (스키마/RLS 변경 불필요 — events 정책은 0004/0012에 이미 존재. **마이그레이션 N/A**.)

- [ ] 실패 테스트 작성 `src/__tests__/eventSheetReadonly.test.tsx`: `initial = { ...evt, visibility:'PERSONAL', owner_id:'partner', version:1 }`, `myId='me'`, `profiles={partner:{displayName:'지민'...}}` → 렌더 후 저장 버튼/삭제 버튼 부재(`queryByRole('button', {name:/저장|삭제/})` null), `상대 일정` 텍스트 + 소유자 이름(`지민`) 노출, 입력들 `readOnly`/`disabled`. SHARED 또는 내 PERSONAL이면 정상 편집 가능(저장 버튼 존재).
- [ ] 실행: `npm run test -- src/__tests__/eventSheetReadonly.test.tsx` → FAIL.
- [ ] 구현 `EventSheet.tsx`:
  - props에 `profiles: ProfileMap` 추가(import type from `@/hooks/useProfiles`). CalendarPage가 이미 보유.
  - derive:
```ts
const isPartnerPersonal = editing && initial != null
  && initial.visibility === 'PERSONAL' && myId != null && initial.owner_id !== myId
const canEdit = !isPartnerPersonal
const ownerName = initial ? profiles[initial.owner_id]?.displayName ?? '상대' : ''
```
  - `isPartnerPersonal`이면 헤더 아래 `<p className={styles.readonlyLabel}>상대 일정 · {ownerName}</p>`, 모든 입력에 `disabled={!canEdit}`(또는 readOnly), 저장/삭제 영역을 `{canEdit && (...)}`로 감쌈.
- [ ] CalendarPage에서 `<EventSheet ... profiles={profiles} />` 전달(prop 추가).
- [ ] 실행: `npm run test -- src/__tests__/eventSheetReadonly.test.tsx` → PASS.
- [ ] `npm run typecheck`. (⚠️ combine-to-typecheck: EventSheet props에 `profiles` 추가는 CalendarPage 호출부 변경 필수 — 두 파일을 같은 typecheck로.)
- [ ] 커밋: `feat(calendar): 상대 PERSONAL 일정 읽기 전용(canEdit·상대 일정 라벨)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 7: 충돌 vs 권한거부 구분 + 시트 유지 + 메모 append-merge

**Files:**
- `src/hooks/useEventMutations.ts:39` (훅 시그니처에 `onPermissionDenied?` 추가) · `:68-78` (update 결과 분기)
- `src/lib/sync/versionedUpdate.ts` (perms 재조회 헬퍼 + `PermissionError` **신규** 클래스 추가; `ConflictError`는 **이미 존재**(`versionedUpdate.ts:7`) → 재정의 금지·재사용)
- `src/pages/CalendarPage.tsx:35` (호출부에 `onPermissionDenied` 배선)
- `src/components/calendar/EventSheet.tsx` (충돌 후 version 재시드 + 메모 머지)
- `src/__tests__/eventConflict.test.tsx` (신규)

조사 03 §2/§3: 0행이 충돌/권한거부/소실 3가지로 모호. UI 가드(Task 6)가 권한거부를 사전 차단하지만, **SHARED 동시편집 충돌** 시 시트 유지 + `expectedVersion` 재시드 + 메모 append-merge(손실 방지, CLAUDE.md §4).

> ⚠️ 현행 `useEventMutations`는 이미 `ConflictError`를 import·사용 중(`useEventMutations.ts:3,72,75,84,87`). 이 Task에서 **새로 추가하는 클래스는 `PermissionError` 하나뿐**이고, `ConflictError`는 그대로 재사용한다(중복 클래스 정의 금지). 현행 훅 시그니처는 `useEventMutations(coupleId, myId, onConflict)`(`:39`)이라 `onPermissionDenied` 파라미터가 없으므로, 아래 멀티라이트 블록의 `onPermissionDenied?.()`가 정의 미존재로 typecheck 실패하지 않도록 **시그니처 확장 + 호출부 배선을 같은 Task 안에서** 처리한다.

- [ ] 실패 테스트 작성 `src/__tests__/eventConflict.test.tsx`: `versionedUpdate` 모킹으로 0행(conflict) 반환 + `refetchEventRow` 모킹. 두 시나리오로 `PermissionError`/`ConflictError` 분기를 못박는다:
  - **버전충돌**: `refetchEventRow`가 `{ visibility:'SHARED', owner_id:'me' }`(또는 내 PERSONAL) 반환 → `update.mutate` 시 `onConflict` 콜백 발화(`ConflictError` 경로), `onPermissionDenied` 미호출. UI에서 (a) 시트가 닫히지 않음(`onClose` 미호출), (b) 재조회된 최신 row의 version으로 시트 `expectedVersion` 갱신(다음 저장이 같은 충돌 반복 안 함), (c) 메모 머지: 내 미저장 메모 + 서버 메모가 다르면 `\n---\n` 구분 append되어 textarea에 반영.
  - **권한거부**: `refetchEventRow`가 `{ visibility:'PERSONAL', owner_id:'partner' }` 반환 → `update.mutate` 시 `onPermissionDenied` 콜백 발화(`PermissionError` 경로), `onConflict` 미호출. (실제 UI에선 Task 6 가드로 상대 PERSONAL의 저장 버튼 자체가 없어 mutate 미도달 — 회귀로 함께 확인하되, 본 분기 테스트는 훅 단위로 두 콜백 분리를 검증.)
- [ ] 실행: `npm run test -- src/__tests__/eventConflict.test.tsx` → FAIL.
- [ ] `versionedUpdate.ts`에 `PermissionError` **신규** 클래스 + 재조회 헬퍼 추가(`ConflictError`는 `:7`에 이미 있으니 건드리지 않음):
```ts
export class PermissionError extends Error {
  constructor(message = '이 일정은 상대만 수정할 수 있어요.') {
    super(message)
    this.name = 'PermissionError'
  }
}

export type FreshRow = { version: number; memo: string | null; visibility: 'SHARED' | 'PERSONAL'; owner_id: string } | null
/** 충돌(0행) 후 현재 서버 행을 id로 재조회. 권한거부 vs 버전충돌 판별·재시드용. */
export async function refetchEventRow(id: string): Promise<FreshRow> {
  const { data, error } = await supabase
    .from('events').select('version, memo, visibility, owner_id').eq('id', id).is('deleted_at', null).maybeSingle()
  if (error) throw new Error(error.message)
  return (data as FreshRow) ?? null
}
```
- [ ] `useEventMutations.ts` **훅 시그니처 확장**: `export function useEventMutations(coupleId: string | null, myId: string | null, onConflict: () => void)` → `..., onConflict: () => void, onPermissionDenied?: () => void)`. `import { PermissionError, refetchEventRow } from '@/lib/sync/versionedUpdate'` 추가(`ConflictError`는 이미 `:3`에서 import 중 — 그대로 둠). `onPermissionDenied`는 optional이라 3-인자 호출부(`RecommendPage.tsx:37`, 테스트 모킹 `eventDeleteUndo.test.tsx`/`calendarDeepLink.test.tsx`/`addCourse.test.ts`)는 변경 불필요.
- [ ] `useEventMutations.ts` update mutation을 권한거부/버전충돌 분기로 교체(verbatim 기반 — `PermissionError`만 신규, `ConflictError` 재사용):
```ts
  const update = useMutation<void, Error, { id: string; expectedVersion: number; patch: EventPatch }>({
    mutationFn: async ({ id, expectedVersion, patch }) => {
      if (!myId) throw new Error('로그인이 필요해요.')
      const res = await versionedUpdate('events', id, expectedVersion, { ...patch, updated_by: myId })
      if (res.status === 'conflict') {
        const fresh = await refetchEventRow(id)
        if (fresh && fresh.visibility === 'PERSONAL' && fresh.owner_id !== myId) {
          throw new PermissionError() // 권한거부 — 메시지 분리(신규 클래스)
        }
        throw new ConflictError() // 진짜 버전충돌(기존 클래스 재사용)
      }
    },
    onError: (err) => {
      if (err instanceof PermissionError) onPermissionDenied?.()
      else if (err instanceof ConflictError) onConflict()
    },
    onSettled: invalidate,
  })
```
- [ ] `CalendarPage.tsx:35` 호출부 배선: `const { create, update, remove } = useEventMutations(coupleId, myId, conflict.flag)` → `..., conflict.flag, () => permission.flag())`(또는 권한거부용 배너/토스트 핸들러). 권한거부 UI는 `aria-live` 배너(`이 일정은 상대만 수정할 수 있어요.`)로 표시하고 시트는 유지(읽기전용 가드 누락 시의 백스톱).
- [ ] `EventSheet.tsx`: `onUpdate` 충돌 후 시트 유지(이미 onSuccess에서만 close — 보존). 충돌 시 부모가 `refetchEventRow` 결과를 시트로 내려 `expectedVersion`/메모 머지하도록 `conflictRefresh?: { version: number; memo: string | null }` prop 추가 → `useEffect`로 version·memo append-merge 반영(`prev !== server ? \`${prev}\n---\n${server}\` : prev`).
- [ ] 실행: `npm run test -- src/__tests__/eventConflict.test.tsx` → PASS.
- [ ] `npm run typecheck`. (⚠️ combine-to-typecheck: `useEventMutations`(시그니처+콜백)·`versionedUpdate`(`PermissionError`/헬퍼)·`EventSheet`(prop)·`CalendarPage`(배선) 4파일 동시 typecheck. `ConflictError`는 재정의 없음 — import 유지.)
- [ ] 커밋: `feat(calendar): 권한거부와 버전충돌 구분 + 시트 유지·version 재시드·메모 머지\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 8: EventSheet 장소 연결 (저장된 장소 피커 → 칩)

**Files:**
- `src/components/calendar/PlacePicker.tsx` (신규)
- `src/components/calendar/EventSheet.tsx` (placeId state + 피커 + 칩)
- `src/__tests__/placePicker.test.tsx` (신규)

조사 04 §1-2: FK/훅/타입 모두 존재, 시트 UI만 부재. `usePlaces`(저장 행, RLS-scope)에서 `place.id` 선택. (스키마/RLS 변경 불필요 — `events.place_id` FK는 0001:197에 존재. **마이그레이션 N/A**.)

- [ ] 실패 테스트 작성 `src/__tests__/placePicker.test.tsx`: `PlacePicker`에 `places=[{id:'p1', name:'성수 카페', address:'...', ...}]` 주입 → 입력 `성수` → 결과에 `성수 카페`, 클릭 → `onPick('p1')` 1회. 선택됨(`selectedId='p1'`)이면 칩 `성수 카페` + 제거(`×`) 버튼 노출, 제거 → `onPick(null)`. 빈/로딩/에러 분기(빈 검색어=전체, 0건=`저장된 장소가 없어요`).
- [ ] 실행: `npm run test -- src/__tests__/placePicker.test.tsx` → FAIL.
- [ ] 구현 `PlacePicker.tsx`(verbatim 패턴: PlaceSearch.tsx:21-84 차용, 저장 행 필터):
```tsx
import { useMemo, useState } from 'react'
import type { PlaceRow } from '@/hooks/usePlaces'
import styles from './PlacePicker.module.css'

type Props = {
  places: PlaceRow[]
  loading: boolean
  selectedId: string | null
  onPick: (id: string | null) => void
}

export function PlacePicker({ places, loading, selectedId, onPick }: Props) {
  const [q, setQ] = useState('')
  const selected = useMemo(() => places.find((p) => p.id === selectedId) ?? null, [places, selectedId])
  const results = useMemo(() => {
    const t = q.trim().toLowerCase()
    const base = t ? places.filter((p) => p.name.toLowerCase().includes(t) || (p.address ?? '').toLowerCase().includes(t)) : places
    return base.slice(0, 8)
  }, [places, q])

  if (selected) {
    return (
      <div className={styles.chip}>
        <span aria-hidden>📍</span>
        <span className={styles.chipLabel}>{selected.name}</span>
        <button type="button" className={styles.chipRemove} aria-label="장소 연결 해제" onClick={() => onPick(null)}>×</button>
      </div>
    )
  }
  return (
    <div className={styles.picker}>
      <input
        type="search" className={styles.input} value={q} placeholder="저장된 장소 연결(선택)"
        aria-label="장소 검색" onChange={(e) => setQ(e.target.value)}
      />
      {loading ? (
        <p className={styles.hint} role="status">불러오는 중…</p>
      ) : results.length === 0 ? (
        <p className={styles.hint}>저장된 장소가 없어요. 장소 탭에서 먼저 저장해보세요.</p>
      ) : (
        <ul className={styles.results}>
          {results.map((p) => (
            <li key={p.id}>
              <button type="button" className={styles.result} onClick={() => onPick(p.id)}>
                <span className={styles.resultName}>{p.name}</span>
                <span className={styles.resultAddr}>{p.region_label ?? p.address}</span>
              </button>
            </li>
          ))}
        </ul>
      )}
    </div>
  )
}
```
  - `PlacePicker.module.css` 신규(토큰 사용: `--c-surface`, `--c-brand`, 44px 터치 타깃).
  - `EventSheet.tsx`: `const [placeId, setPlaceId] = useState<string | null>(initial?.place_id ?? null)`(Task 5의 임시 상수 대체), `usePlaces(coupleId)` 호출 또는 props로 `places`/`placesLoading` 주입(focus-trap 안에 들어가야 함 — 조사 04 §a11y). 메모 위에 `<PlacePicker places=... loading=... selectedId={placeId} onPick={setPlaceId} />`.
- [ ] 실행: `npm run test -- src/__tests__/placePicker.test.tsx` → PASS.
- [ ] `npm run typecheck`. (⚠️ combine-to-typecheck: EventSheet에 places props 추가 시 CalendarPage 호출부 동시.)
- [ ] 커밋: `feat(calendar): EventSheet 장소 연결 피커(저장 장소→place_id·칩)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 9: EventSheet reduce-motion + 스와이프 다운 닫기

**Files:**
- `src/components/calendar/EventSheet.tsx` (백드롭/시트 드래그 핸들러)
- `src/components/calendar/EventSheet.module.css` (transition reduce-motion 분기)
- `src/__tests__/eventSheetGesture.test.tsx` (신규)

ux-and-accessibility §1/§5: 시트 스와이프 다운 닫기(버튼 대체 경로 = 기존 X/Esc 유지), reduce-motion 시 슬라이드 제거.

- [ ] 실패 테스트 작성 `src/__tests__/eventSheetGesture.test.tsx`: 시트 헤더에 `touchstart`(y=100)→`touchmove`(y=260, 임계 초과)→`touchend` 시 `onClose` 호출. 임계 미만(y=110)은 닫히지 않음. (jsdom Touch 이벤트 합성.) reduce-motion 분기는 CSS라 스냅샷/클래스 존재로 확인(`@media (prefers-reduced-motion)`는 e2e에서 시각 확인 — 단위는 핸들러만).
- [ ] 실행: `npm run test -- src/__tests__/eventSheetGesture.test.tsx` → FAIL.
- [ ] 구현 `EventSheet.tsx`: `onTouchStart/Move/End`로 dragY 추적, `dragY > 120` 이고 본문 scrollTop===0이면 `onClose()`; 그 외 스냅백. `touch-action` CSS는 핸들 영역 `none`. 닫기 버튼/Esc 경로 유지(발견성 — 제스처 단독 금지).
- [ ] `EventSheet.module.css`에 `@media (prefers-reduced-motion: reduce) { .sheet { transition: none; transform: none; } }` 추가(슬라이드/스프링 제거, fade 또는 즉시).
- [ ] 실행: `npm run test -- src/__tests__/eventSheetGesture.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): EventSheet 스와이프 다운 닫기 + reduce-motion 전환 축소\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## Phase R2.3: CalendarPage — 범위 시트·월칩·주/일 뷰·스켈레톤·범례

### Task 10: 반복 범위 선택 시트 (이 일정만/이후/전체) 배선

**Files:**
- `src/components/calendar/ScopeSheet.tsx` (신규)
- `src/pages/CalendarPage.tsx:101-161` (편집/삭제 → 범위 분기)
- `src/components/calendar/ScopeSheet.module.css` (신규)
- `src/__tests__/recurrenceScopeApply.test.tsx` (신규, CalendarPage 배선 통합)

조사 01 §1/§6: occurrence 편집/삭제 시 반복이면 범위 시트. `이 일정만 삭제`=EXDATE; `이 일정만 수정`=EXDATE+override INSERT(owner_id=myId); `이후`=시리즈 절단+새 시리즈; `전체`=기존 plain update/softDelete. (스키마/RLS 변경 불필요 — override는 평범한 비반복 events 행, `events_select`로 가시·`events_insert`로 owner_id=auth.uid() 충족. **마이그레이션 N/A**.)

- [ ] 실패 테스트 작성 `src/__tests__/recurrenceScopeApply.test.tsx`: 반복(WEEKLY) occurrence 삭제 → ScopeSheet 노출(`이 일정만`/`이후`/`전체`). `이 일정만` 클릭 → 시리즈 `update`가 `recurrence_rule`에 그 occurrence dayKey EXDATE append 호출(softDelete 아님), `expectedVersion=시리즈 version`. `이후` 클릭 → 시리즈 `update`(truncatedRule) + 새 시리즈 `create`(start=분할일·rule=원 freq). `전체` 클릭 → 시리즈 `softDelete`. occurrence 수정도 동일 분기: `이 일정만`=EXDATE update + override create(recurrenceRule:null, owner_id=myId, 변경 필드).
- [ ] 실행: `npm run test -- src/__tests__/recurrenceScopeApply.test.tsx` → FAIL.
- [ ] 구현 `ScopeSheet.tsx`: `role="dialog"` + 3버튼(`이 일정만`/`이후 모두`/`전체`) + 취소. props `{ mode: 'edit'|'delete'; onPick: (scope:'this'|'following'|'all')=>void; onCancel }`. 비반복이면 시트 생략(직접 적용).
- [ ] CalendarPage: occurrence 클릭 시 `_seriesStart`/`_seriesEnd`와 클릭 occurrence dayKey/ISO 보존(조사 01 §3 — 현재 line 161은 시리즈로 덮어써 occurrence 소실 → occurrence ISO 별도 state로 전달). 삭제/수정 핸들러에서:
  - `this` 삭제: `update({ id: series.id, expectedVersion: series.version, patch: { recurrence_rule: exdateOccurrence(rule, occDayKey) } })`.
  - `this` 수정: 위 EXDATE update **성공 후**(순서 — 조사 01 §6 부분실패 방어) `create({ ...override fields, recurrenceRule: null, placeId, ... })`(owner_id=myId via create). 둘 다 Undo 토스트 옵션.
  - `following`: `const {truncatedRule, newSeriesStartKey} = splitFollowing(rule, occStartIso)` → 시리즈 `update(recurrence_rule:truncatedRule)` + `create(새 시리즈, start=분할 occStart, recurrenceRule = buildRule(freq,interval,...))`.
  - `all`: 기존 `softDelete`/`update`(시리즈 plain) — 현행 유지.
  - 부분 실패(EXDATE 성공·override 실패): conflict 배너 + 사용자 메시지.
- [ ] 실행: `npm run test -- src/__tests__/recurrenceScopeApply.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 반복 일정 3-범위 시트(이 일정만 EXDATE·이후 분할·전체)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 11: 월 셀 제목 칩 (+N more, 색+심볼 이중화)

**Files:**
- `src/pages/CalendarPage.tsx:214-270` (MonthGrid)
- `src/pages/CalendarPage.module.css` (셀 칩 레이아웃)
- `src/__tests__/monthChips.test.tsx` (신규)

조사 01 §4: 월 셀이 트랙 점만 → 제목 칩 1-2개 + `+N`. `grouped[c.key]`가 occurrence 제공. 중첩 버튼 회피(칩은 비버튼 span, 셀 버튼 탭 유지).

- [ ] 실패 테스트 작성 `src/__tests__/monthChips.test.tsx`: 한 날에 이벤트 3개 → 해당 셀에 제목 칩 2개 노출 + `+1`(overflow). 칩에 트랙 심볼(●/▲/■)이 텍스트로 동반(색 단독 금지 §8). aria-label은 기존 count+트랙 유지.
- [ ] 실행: `npm run test -- src/__tests__/monthChips.test.tsx` → FAIL.
- [ ] 구현 `MonthGrid`(verbatim 기반): `.dots` 자리에 `evs.slice(0,2)` 제목 칩(`<span className={styles.chip} style={{ '--chip': TRACK_META[deriveTrack(e,myId)].cssVar }}>{TRACK_META[t].symbol} {e.title}</span>`) + `evs.length>2 && <span className={styles.chipMore}>+{evs.length-2}</span>`. 셀 `<button>` 유지, 칩은 비인터랙티브 span. CSS: 셀 `aspect-ratio` 완화(min-height), 칩 truncate(ellipsis).
- [ ] 실행: `npm run test -- src/__tests__/monthChips.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 월 셀 제목 칩(+N more·색+심볼 이중화)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 12: 요일(타임라인) 뷰 + 뷰 전환 (주 뷰 가능 시)

**Files:**
- `src/components/calendar/DayTimeline.tsx` (신규)
- `src/components/calendar/WeekStrip.tsx` (신규, 주 네비)
- `src/pages/CalendarPage.tsx` (view 모드 state + `?view=` 파라미터)
- `src/pages/CalendarPage.module.css`
- `src/__tests__/dayTimeline.test.tsx` (신규)

조사 04 §4: Day 타임라인 부재(현재 flat agenda). `minuteOfDay`(Task 3)로 시간 위치 + 종일 상단 밴드. 뷰 전환 `월/주/일` + `?view=` 딥링크.

- [ ] 실패 테스트 작성 `src/__tests__/dayTimeline.test.tsx`: `DayTimeline`에 occurrence 2개(10:00, 14:30) + 종일 1개 주입 → 종일 이벤트는 상단 종일 밴드(`all-day lane`)에, 시간 이벤트는 top px가 `minuteOfDay`에 비례(10:00 < 14:30 위치). 빈 날 → `이 날 일정이 없어요` CTA. composite key(`${id}:${start}`) 사용(반복 occurrence 키 충돌 방지 — 조사 01 §4).
- [ ] 실행: `npm run test -- src/__tests__/dayTimeline.test.tsx` → FAIL.
- [ ] 구현:
  - `DayTimeline.tsx`: 24시간 hour rows + 종일 밴드. 각 timed occurrence `top = minuteOfDay(ev.start)/1440*100%`, `height = (minuteOfDay(ev.end)-minuteOfDay(ev.start))` 비례(자정 넘김은 당일 끝까지 클램프). `deriveTrack`/`TRACK_META` 색, onEdit는 occurrence 전달(범위 시트 연계). 빈 상태 CTA `＋`.
  - `WeekStrip.tsx`: `weekMatrix(selected)` 7일 칩 + 좌우 주 이동(`diffDays`). (주 뷰가 비대해지면 일 뷰만 출하하고 주는 strip 네비로 — tractable 범위.)
  - CalendarPage: `const [mode, setMode] = useState<'month'|'week'|'day'>(...)`, `?view=` 동기화(딥링크/뒤로가기). 뷰 전환 세그먼트 컨트롤(월/주/일, `aria-pressed`). day 모드면 `DayTimeline`, 그 외 `MonthGrid`+`DayAgenda` 또는 `WeekStrip`.
- [ ] 실행: `npm run test -- src/__tests__/dayTimeline.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 요일 타임라인 뷰 + 월/주/일 전환(?view 딥링크)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 13: 캘린더 로딩 스켈레톤 + 연결됨-빈 vs 미연결 구분 + 빈 아젠다 CTA + 장소 칩/지도 링크

**Files:**
- `src/pages/CalendarPage.tsx:30-90` (isLoading 노출), DayAgenda 빈 상태/장소 칩
- `src/hooks/useEvents.ts` (isLoading 노출 확인)
- `src/__tests__/calendarStates.test.tsx` (신규)

조사 04 §5: 캘린더에 스켈레톤 없음(`useEvents().isLoading` 미사용 → 빈 캘린더). 미연결=`EmptyState`(있음), 연결됨-빈=bare `<p>`(개선). 빈 아젠다 add-event CTA. 아젠다 장소 칩(place_id→PlaceRow) + 지도 링크.

- [ ] 실패 테스트 작성 `src/__tests__/calendarStates.test.tsx`:
  - 로딩(`events isLoading=true`, couple ACTIVE) → `role="status"`(Skeleton) 노출, 월 그리드 대신 스켈레톤.
  - 미연결(`couple.status!=='ACTIVE'`) → 기존 `먼저 상대와 연결해요` EmptyState(회귀 유지).
  - 연결됨-빈(ACTIVE, events 0) → 아젠다 빈 상태가 `EmptyState`(이모지+title+action CTA `＋ 일정 추가`)로, action 클릭 시 시트 open.
  - 장소 연결된 이벤트(place_id='p1', places=[{id:'p1',name:'성수 카페',lat,lng}]) → 아젠다 항목에 장소 칩(`성수 카페`) + 지도 링크(`/?place=p1` 또는 좌표 링크).
- [ ] 실행: `npm run test -- src/__tests__/calendarStates.test.tsx` → FAIL.
- [ ] 구현:
  - `useEvents` 반환에 `isLoading` 사용(CalendarPage에서 `const { data: events, isLoading: eventsLoading } = useEvents(coupleId)`). couple ACTIVE && eventsLoading → `<Skeleton count={6} label="일정 불러오는 중" />` (그리드 모양 placeholder).
  - DayAgenda 빈 분기: bare `<p>` → `<EmptyState emoji="🗓️" title="이 날 일정이 없어요" action={<button onClick={openCreate}>＋ 일정 추가</button>} />`.
  - 장소 칩: CalendarPage에서 `usePlaces(coupleId)` → `placeById` map, DayAgenda에 주입. 각 항목 `ev.place_id`면 `<a className={styles.placeChip} href={`/?place=${ev.place_id}`}>📍 {place.name}</a>`(지도 링크). `MapPage`의 `?place=` 포커스 처리는 R4/후속 — 여기선 딥링크 발신만(조사 04 §3: 신규 플러밍, 칩+링크는 출하, 포커스 수신은 후속 메모).
- [ ] 실행: `npm run test -- src/__tests__/calendarStates.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 로딩 스켈레톤·연결됨-빈 EmptyState CTA·아젠다 장소 칩/지도링크\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

### Task 14: 트랙 범례 색+이름칩 (비인터랙티브 legend)

**Files:**
- `src/components/calendar/TrackLegend.tsx` (신규)
- `src/lib/calendar/track.ts:14-20` (라벨 확인 — `mine`='나')
- `src/pages/CalendarPage.tsx` (legend 마운트)
- `src/__tests__/trackLegend.test.tsx` (신규)

조사 04 §6: `TrackChips`는 필터(인터랙티브). 스펙 R2는 명확성용 색+이름칩(함께/내 일정). 기존 라벨 `함께/나/상대` → 범례는 `함께`/`내 일정`(authoring 2트랙) 표기, viewer의 `상대`는 별도. (라벨 불일치 의식적 처리: TRACK_META는 `나` 유지, 범례 컴포넌트에서 `mine`을 `내 일정`으로 표기.)

- [ ] 실패 테스트 작성 `src/__tests__/trackLegend.test.tsx`: `TrackLegend` 렌더 → `함께`(●) + `내 일정`(▲) 칩, 각 색(cssVar) + 심볼 + 텍스트 동반(색 단독 금지). 비인터랙티브(`role` 없이 `<ul>`/`<li>`, 버튼 아님). `aria-hidden` 심볼.
- [ ] 실행: `npm run test -- src/__tests__/trackLegend.test.tsx` → FAIL.
- [ ] 구현 `TrackLegend.tsx`:
```tsx
import { TRACK_META } from '@/lib/calendar/track'
import styles from './TrackLegend.module.css'

const LEGEND: { track: 'shared' | 'mine'; label: string }[] = [
  { track: 'shared', label: '함께' },
  { track: 'mine', label: '내 일정' },
]

export function TrackLegend() {
  return (
    <ul className={styles.legend} aria-label="일정 트랙 범례">
      {LEGEND.map(({ track, label }) => {
        const meta = TRACK_META[track]
        return (
          <li key={track} className={styles.item}>
            <span className={styles.swatch} style={{ background: meta.cssVar }} aria-hidden />
            <span aria-hidden>{meta.symbol}</span>
            <span className={styles.label}>{label}</span>
          </li>
        )
      })}
    </ul>
  )
}
```
  - `TrackLegend.module.css` 신규(토큰·다크모드 대비). CalendarPage 헤더 근처(`TrackChips` 위/아래) 마운트.
- [ ] 실행: `npm run test -- src/__tests__/trackLegend.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(calendar): 트랙 범례 색+이름칩(함께/내 일정·색+심볼 이중화)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## Phase R2.4: 다가오는 일정 피드 (인앱 알림 1차 수단)

### Task 15: `UpcomingFeed` 카드 (TodayCard 승격) + now 틱 + Map 마운트

**Files:**
- `src/components/common/TodayCard.tsx` → `src/components/common/UpcomingFeed.tsx` (확장/개명)
- `src/components/common/UpcomingFeed.module.css` (TodayCard.module.css 확장)
- `src/pages/MapPage.tsx:30-33` (카드 마운트)
- `src/__tests__/upcomingFeedCard.test.tsx` (신규)

조사 02 §4/§5: IA 4탭 고정 → 새 탭 금지, **카드**. 미사용 `TodayCard` 승격. `buildUpcomingFeed`(Task 4) + `setInterval`(now 틱) + Realtime 무료(useEvents). 빈 시 self-hide(죽은 카드 금지). 푸시는 PWA+iOS16.4 후속(메모).

- [ ] 실패 테스트 작성 `src/__tests__/upcomingFeedCard.test.tsx`: `UpcomingFeed`(events·myId 주입, fake timers) → `D-3` 항목 + `곧 시작: N분 뒤` 항목 렌더, `aria-live="polite"` 영역에 imminent. 항목 0이면 카드 미렌더(null). `prefers-reduced-motion`이면 카운트업 생략(즉시 최종값). icon+text 동반(🔔/D-day, 색 단독 금지).
- [ ] 실행: `npm run test -- src/__tests__/upcomingFeedCard.test.tsx` → FAIL.
- [ ] 구현 `UpcomingFeed.tsx`(TodayCard 기반 확장):
```tsx
import { useEffect, useState } from 'react'
import { useEvents } from '@/hooks/useEvents'
import { buildUpcomingFeed } from '@/lib/calendar/upcomingFeed'
import { dDayLabel } from '@/lib/calendar/eventDays'
import styles from './UpcomingFeed.module.css'

export function UpcomingFeed({ coupleId, myId }: { coupleId: string | null; myId: string | null }) {
  const { data: events } = useEvents(coupleId)
  const [now, setNow] = useState(() => new Date().toISOString())
  useEffect(() => {
    const t = setInterval(() => setNow(new Date().toISOString()), 60000) // 1분 틱(곧 시작 카운트다운)
    return () => clearInterval(t)
  }, [])
  const items = buildUpcomingFeed(events ?? [], now, myId)
  if (items.length === 0) return null // 죽은 카드 금지(self-hide)
  return (
    <section className={styles.card} aria-label="다가오는 일정">
      <h2 className={styles.head}>다가오는 일정</h2>
      <ul className={styles.list}>
        {items.map((i) => (
          <li key={i.id} className={styles.item} {...(i.kind === 'imminent' ? { 'aria-live': 'polite' } : {})}>
            <span className={styles.badge} aria-hidden>{i.kind === 'imminent' ? '🔔' : '📅'}</span>
            <span className={styles.label}>{i.label}</span>
            <span className={styles.title}>{i.title}</span>
          </li>
        ))}
      </ul>
    </section>
  )
}
```
  - `void dDayLabel` import는 필요시 제거(미사용 금지 — TS noUnusedLocals). 헤더 주석에 "웹푸시는 PWA 홈화면+iOS16.4 후속, 인앱 피드가 1차 알림 수단" 명시.
  - `MapPage.tsx`: conflict banner와 지도 사이에 `<UpcomingFeed coupleId={coupleId} myId={myId} />` 마운트.
  - 구 `TodayCard`/CSS 개명(또는 re-export 정리, 죽은 파일 금지).
- [ ] 실행: `npm run test -- src/__tests__/upcomingFeedCard.test.tsx` → PASS.
- [ ] `npm run typecheck`.
- [ ] 커밋: `feat(map): 다가오는 일정 인앱 피드 카드(TodayCard 승격·now 틱·self-hide)\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## Phase R2.5: 게이트 검증

### Task 16: 전체 게이트(typecheck/test/build/e2e) + 비주얼 스모크 재생성

**Files:**
- `e2e/` (캘린더 스냅샷: 월 칩·일 타임라인·빈/로딩·범례·피드 카드)
- 변경 없음(검증 전용)

CLAUDE.md §6 제출 게이트 전량. RLS/EXIF 변경 없음 명시.

- [ ] `npm run typecheck` → 0 에러.
- [ ] `npm run test` → 전체 PASS(신규 단위/통합 포함).
- [ ] `npm run build` → 성공(`tsc --noEmit && vite build`).
- [ ] e2e 스냅샷 갱신 필요 화면 추가/재생성 후 `npm run e2e` → green(월 셀 칩, 일 타임라인, 로딩 스켈레톤, 연결됨-빈 CTA, 트랙 범례, 다가오는 피드 카드 — 라이트/다크 + 빈/로딩).
- [ ] 게이트 N/A 명시: **RLS 격리** = events RLS(0004/0012) 변경 없음(override는 기존 정책 충족) → 신규 마이그레이션 없음. **EXIF 스트립** = 발행 무관 → N/A. **접근성 회귀 없음** = 색+심볼+라벨 이중화·`aria-live`·reduce-motion·focus-trap 유지 확인.
- [ ] 커밋: `test(calendar): R2 게이트(typecheck·vitest·build·e2e) + 비주얼 스모크 갱신\n\nCo-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`

---

## 정직성(검증 한계)

- 스와이프 다운 닫기·reduce-motion 슬라이드 손맛은 jsdom으로 핸들러만 검증 → 실기기 육안 확인이 남음(라운드 종료 시 명시).
- `?place=` 지도 포커스 **수신**은 후속(이 라운드는 칩+링크 발신까지). MapPage 포커스 처리는 R4 또는 별도 태스크.
- 멀티라이트 범위 연산(EXDATE+override, 시리즈 절단+새 시리즈)은 트랜잭션 RPC 없음 → 순서(EXDATE 먼저) + 부분실패 conflict 배너로 방어. 완전 원자성은 후속 Edge Function/RPC 후보(조사 01 §6).
- 웹푸시는 미구현(PWA 홈화면+iOS16.4 필요) — 인앱 피드가 1차 알림 수단(설계서 트레이드오프).
- **타임존(여행 현지시각) — 이 라운드(R2) 범위 밖, R4(감/일관성/a11y)의 캘린더 후속 태스크가 소유.** 스펙(`2026-06-16-ux-overhaul-design.md:48`)은 R2 항목으로 "타임존(여행 현지시각)"을 명시하지만, 본 R2 계획은 **고정 표시 tz(Asia/Seoul, +09:00)** 위에 순수 함수(`buildEventTimes`/`minuteOfDay`/`weekMatrix`)와 일-버킷팅(`dayKey`)을 세워 두 단말의 날짜 버킷 일치(§5.1 "타임존 어긋남 0")를 먼저 확보한다. **데이터는 이미 존재**한다: `EventRow.time_zone`(`useEvents.ts:13`)이 생성 시 채워지지만(현재 `DISPLAY_TZ`로) 어디에서도 **이벤트별로 표시되지 않는다.** 이벤트별 현지시각 표시는 (a) `EventSheet`/`DayAgenda`/`DayTimeline`에 저장된 `time_zone` 노출(예: 표시 tz≠KST일 때 "현지 14:00 (KST 16:00)" 라벨), (b) `buildEventTimes`/`minuteOfDay`/`formatTime`이 하드코딩 KST 대신 **이벤트별 tz**를 받도록 시그니처 일반화(현재 `minuteOfDay`는 `tz=DISPLAY_TZ` 기본값으로 후방호환 가능, `buildEventTimes`의 `iso()`는 `+09:00` 하드코딩 → tz 인자화 필요), (c) `EventSheet`에 tz 선택기 추가 + `timeZone: DISPLAY_TZ` 고정 대신 사용자 선택값 사용을 묶는 **별도 태스크(R4 "캘린더 이벤트별 타임존")**로 분리한다. R2가 이를 의식적으로 미루는 이유: 위 (a)~(c)는 R2의 모든 시각 헬퍼/시트/뷰 시그니처를 가로질러 바꾸므로(반복·범위·피드 태스크와 충돌), 일-버킷 일치라는 코어를 먼저 안정화한 뒤 한 태스크로 다루는 편이 안전하다. (요약: R2는 단일-tz 가정을 **유지**하되 본 줄로 명시적으로 차기 라운드에 위임.)
